{
  "name": "spotify-yt-player",
  "version": "1.0.0",
  "description": "A Spotify-like YouTube Music desktop app",
  "author": "iisuperboyii",
  "main": "electron/main.js",
  "scripts": {
    "dev:react": "npm run dev --prefix renderer",
    "dev:electron": "electron .",
    "dev": "concurrently \"npm run dev:react\" \"npm run dev:electron\"",
    "build:renderer": "npm run build --prefix renderer",
    "build": "npm run build:renderer && electron-builder --dir",
    "dist": "npm run build:renderer && electron-builder --dir && powershell -Command \"Compress-Archive -Path 'release/win-unpacked/*' -DestinationPath 'release/SpotifyYTPlayer-win-x64.zip' -Force\" && echo Done! ZIP at release/SpotifyYTPlayer-win-x64.zip"
  },
  "build": {
    "appId": "com.iisuperboyii.spotify-yt",
    "productName": "Spotify YT Player",
    "npmRebuild": false,
    "directories": {
      "output": "release"
    },
    "files": [
      "electron/**/*",
      "renderer/dist/**/*",
      "node_modules/**/*",
      "package.json"
    ],
    "extraResources": [],
    "win": {
      "target": [
        {
          "target": "dir",
          "arch": ["x64"]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "concurrently": "^8.2.2",
    "electron": "^30.0.0",
    "electron-builder": "^25.1.8"
  },
  "dependencies": {
    "win-audio": "^3.0.5"
  }
}
